package com.Kenji.pagadvisor.ui.theme

import androidx.compose.ui.graphics.Color

/// --- Cores Primárias ---
val PagYellow = Color(0xFFF5B615) // Botões, Destaques, Contornos
val PagWhite = Color(0xFFFFFFFF)  // Fundo dos Cards, Texto dos Botões
val PagBlack = Color(0xFF000000)  // Texto principal
val PagDarkGray = Color(0xFF333333) // Texto principal (alternativa)

// --- Cores Secundárias ---
val PagLightGray = Color(0xFFF0F0F0) // Fundo de Chips (não selecionado)
val PagMediumGray = Color(0xFFD3D3D3) // Borda de Chips (não selecionado)

// --- Cores do Gradiente ---
val PagGradientTop = Color(0xFFDDE460)
val PagGradientBottom = Color(0xFFA7DCDA)