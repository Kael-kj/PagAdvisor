package com.Kenji.pagadvisor.ui.theme

import androidx.compose.ui.graphics.Color

/// --- Cores Primárias ---
val PagYellow = Color(0xFFF5B615) // Botões, Destaques, Contornos
val PagWhite = Color(0xFFFFFFFF)  // Fundo dos Cards, Texto dos Botões
val PagBlack = Color(0xFF000000)  // Texto principal
val PagDarkGray = Color(0xFF545454) // Texto principal (alternativa)

// --- Cores Secundárias ---
val PagLightGray = Color(0xFFE3E3E3) // Fundo de Chips (não selecionado)
val PagMediumGray = Color(0xFFDADADA) // Borda de Chips (não selecionado)

// --- Cores do Gradiente ---
val PagGradientTop = Color(0xFFDDE460)
val PagGradientBottom = Color(0xFFA7DCDA)

val PagVerde = Color(0xFF0EAFA0)

val PagAzul = Color(0xFF0078AD)


